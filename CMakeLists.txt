
set (qs_manager_files 
	has_member.hpp
	qs_manager.h)

	install(FILES ${qs_manager_files} DESTINATION "${INSTALL_INCLUDE_DIR}/core/utils/qs_manager")
if(DEFINED ${INSTALL_INCLUDE_DIR})
	message(FATAL_ERROR "L")
	
endif()